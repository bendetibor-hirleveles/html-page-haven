name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔽 Repo klónozása
        uses: actions/checkout@v4

      - name: 🟢 Node.js környezet beállítása
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📦 Függőségek telepítése
        run: npm install

      - name: 🛠 React app buildelése
        run: npm run build

      - name: 📁 Fájlok előkészítése feltöltéshez
        run: |
          rm -rf deploy_upload
          mkdir deploy_upload

          # React build eredménye
          cp -rv dist/* deploy_upload/

          # Gyökér HTML fájlok
          find . -maxdepth 1 -name "*.html" -exec cp -v {} deploy_upload/ \;

          # Teljes mappák
          mkdir -p deploy_upload/assets
          cp -rv assets/* deploy_upload/assets/ || echo "⚠️ assets mappa nem létezik vagy üres"

          cp -rv blog deploy_upload/blog || echo "⚠️ blog nem található vagy üres"
          cp -rv supabase deploy_upload/supabase || echo "⚠️ supabase nem található vagy üres"
          cp -rv public deploy_upload/public || echo "⚠️ public nem található vagy üres"
          cp -rv src deploy_upload/src || echo "⚠️ src nem található vagy üres"

          # .htaccess is kellhet a routinghoz
          cp -v .htaccess deploy_upload/.htaccess || echo "⚠️ .htaccess nem található"

          echo "✅ deploy_upload tartalma:"
          find deploy_upload -type f

      - name: 🚀 Fájlok feltöltése FTP-n (passzív mód)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: deploy_upload/
          server-dir: /public_html/
          include-dotfiles: true
          dangerous-clean-slate: false
          delete: false
          passive: true

