Options -Indexes
DirectoryIndex index.html

# 🔒 Biztonsági fejlécek
Header always set X-Frame-Options DENY
Header always set X-Content-Type-Options nosniff
Header always set X-XSS-Protection "1; mode=block"
Header always set Referrer-Policy "strict-origin-when-cross-origin"

# 🚧 Ne robbanjon le, ha az admin vagy auth fájl hiányzik
RewriteEngine On

# ADMIN
RewriteCond %{REQUEST_URI} ^/admin
RewriteCond %{DOCUMENT_ROOT}/admin%{REQUEST_URI} !-f
RewriteCond %{DOCUMENT_ROOT}/admin%{REQUEST_URI} !-d
RewriteRule ^admin(/.*)?$ /admin [L]

# AUTH
RewriteCond %{REQUEST_URI} ^/auth
RewriteCond %{DOCUMENT_ROOT}/auth%{REQUEST_URI} !-f
RewriteCond %{DOCUMENT_ROOT}/auth%{REQUEST_URI} !-d
RewriteRule ^auth(/.*)?$ /auth [L]
